{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "mekhq gradle build",
            "type": "shell",
            "command": "gradle build -p mekhq --no-daemon",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "mm gradle build",
            "type": "shell",
            "command": "gradle build -p megamek --no-daemon",
            "problemMatcher": [],

        },
        {
            "label": "megameklab gradle build",
            "type": "shell",
            "command": "gradle build -p megameklab --no-daemon",
            "problemMatcher": [],

        }
    ]
}